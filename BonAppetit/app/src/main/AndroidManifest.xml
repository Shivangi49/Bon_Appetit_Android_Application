<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bonappetit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.OrderActivity"></activity>
        <activity android:name=".Admin.ViewPayment" />
        <activity android:name=".Admin.ViewFeedback" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".Admin.ManageCategory" />
        <activity android:name=".FoodList" />
        <activity android:name=".Admin.AdminActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".Admin.ManageFood" />
        <activity
            android:name=".HomeActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Admin.AdminLoginActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>